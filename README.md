# Technical Test Junior Fullstack Engineer

Multi-Step Invoice/Resi Generator (Fleet & Logistics)

## Docker
Menjalankan Program
```bash
  docker compose --file=cmd/docker-compose.yml build
  docker compose --file=cmd/docker-compose.yml up -d 
```
Menghapus Data (Volumes)
```bash
docker compose --file=cmd/docker-compose.yml down --volumes
```